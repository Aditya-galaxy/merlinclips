node_modules/
dist/
.env
.env.*
!.env.example
*.tsbuildinfo
__pycache__/
*.pyc

# working context, not documentation for anyone else
HANDOFF.md

# planning + strategy, not documentation for anyone else
docs/

# bun project — npm lockfiles are not ours
package-lock.json

# Internal design notes — not part of the shipped repo
DESIGN.md

# Internal operations notes — not part of the public repo
RUNBOOK.md

# Local-only credential scratch files
check-*.local.ts
